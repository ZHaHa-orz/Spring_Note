spring:
  data:
    redis:
      host: localhost
      port: 6379
      # password:
      database: 0 # 默认数据库
      lettuce:
        pool: # 连接池配置
          max-active: 8 # 最大连接
          max-idle: 8 # 最大空闲连接
          min-idle: 0 # 最小空闲连接
          max-wait: 100 # 连接等待时间